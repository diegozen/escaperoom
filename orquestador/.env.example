# ── Orquestador — variables de entorno ───────────────────────
# Copia este archivo a .env y rellena los valores reales.
# NUNCA subas el .env real al repositorio.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=escape_db
DB_USER=escape_user
DB_PASSWORD=CAMBIA_ESTO

# Opcional: nivel de log de SQLAlchemy (true = verbose, false = silencioso)
# SQL_ECHO=false

ALLOWED_ORIGINS=http://localhost,http://127.0.0.1
SSH_HOST=localhost